<section class="main-page">
  <div class="page-width main-page__content">
    <div class="eyebrow">{{ page.title }}</div>
    <h1 class="section-heading">{{ page.title }}</h1>
    <div class="rte">{{ page.content }}</div>
  </div>
</section>

{% schema %}
{
  "name": "Main page",
  "settings": []
}
{% endschema %}
